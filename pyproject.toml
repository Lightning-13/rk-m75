[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rkm75"
version = "0.1.0"
description = "Open-source RGB control library for the RK M75"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["rkm75*"]